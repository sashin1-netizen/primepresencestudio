# Required only after the owner confirms the canonical production origin.
# Use an absolute HTTPS URL with no trailing slash; never deploy a placeholder.
NEXT_PUBLIC_SITE_URL=

# Server-only preview controls. Preview mode emits noindex and robots disallow.
PRIVATE_PREVIEW_MODE=false
PRIVATE_PREVIEW_INCLUDE_DRAFTS=false
