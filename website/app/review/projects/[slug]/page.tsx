import type { Metadata } from "next"; import { notFound } from "next/navigation"; import PageShell from "@/components/PageShell"; import { ownerContent } from "@/content/owner-content.mjs"; import { privatePreviewEnabled, previewDraftsEnabled } from "@/lib/preview";
export const metadata: Metadata = { title: "Draft project review", robots: { index: false, follow: false, noarchive: true } };
export function generateStaticParams(){ return privatePreviewEnabled && previewDraftsEnabled ? ownerContent.projects.map(project=>({slug:project.slug})) : []; }
export const dynamicParams = false;
export default async function DraftProjectReview({params}:{params:Promise<{slug:string}>}){ if(!privatePreviewEnabled||!previewDraftsEnabled) notFound(); const {slug}=await params; const project=ownerContent.projects.find(item=>item.slug===slug); if(!project) notFound(); return <PageShell eyebrow={`Private ${project.publicationStatus} review`} title={project.clientName || project.slug} intro={project.summary || "Summary not supplied."}><dl className="grid gap-6 md:grid-cols-2">{Object.entries({Industry:project.industry,Services:project.services.join(", "),Challenge:project.challenge,"Strategic direction":project.strategicDirection,"Creative execution":project.creativeExecution,Results:project.results,"Media rights":project.mediaRightsConfirmed?"Confirmed":"Missing","Client approval":project.clientPublicationApproved?"Confirmed":"Missing","Approval date":project.approvalDate}).map(([label,value])=><div className="surface p-6" key={label}><dt className="eyebrow">{label}</dt><dd className="mt-3 whitespace-pre-wrap text-[var(--muted)]">{value||"Not supplied"}</dd></div>)}</dl></PageShell>; }
