import Link from "next/link"; import PageShell from "@/components/PageShell"; import Contact from "@/components/Contact"; import { services } from "@/content/services"; import { contactDetails } from "@/content/site";
export const metadata = { title: "Branding & Website Services", description: "Logo design, brand identity, strategy, reveal video and premium website services for South African businesses.", alternates: { canonical: "/services" } };
export default function Page() { return <PageShell after={<Contact contact={contactDetails} />} eyebrow="Services" title="A stronger presence, built as a system." intro="Choose a focused deliverable or combine strategy, identity and digital execution into one coherent engagement."><div className="grid gap-px overflow-hidden rounded-[var(--radius-md)] bg-white/10 md:grid-cols-2">{services.map(s => <article key={s.slug} className="group flex flex-col bg-[#080808] p-8 transition-colors hover:bg-[#0c0b09] sm:p-10"><h2 className="font-[family-name:var(--font-cormorant)] text-3xl">{s.title}</h2><p className="mt-4 leading-7 text-gray-300">{s.summary}</p><p className="eyebrow mt-7">Typical output</p><p className="mt-3 text-sm leading-7 text-gray-400">{s.deliverables}</p><Link href="/contact" className="link-arrow mt-7 inline-flex self-start">Discuss this service <span aria-hidden="true">→</span></Link></article>)}</div></PageShell> }
