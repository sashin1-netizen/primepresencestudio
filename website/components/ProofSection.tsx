import Image from "next/image";
import { publishedFounders, publishedTeamMembers, publishedTestimonials } from "@/content/proof";
export default function ProofSection() { const people = [...publishedFounders, ...publishedTeamMembers]; if (!people.length && !publishedTestimonials.length) return null; return <section className="mt-16 border-t border-white/10 pt-16"><p className="eyebrow">People and proof</p>{people.length ? <div className="mt-8 grid gap-6 md:grid-cols-2">{people.map(person => <article key={person.id} className="surface overflow-hidden">{person.image && <div className="relative aspect-[4/3]"><Image src={person.image.src} alt={person.image.alt} fill sizes="(min-width:768px) 50vw, 100vw" className="object-cover" /></div>}<div className="p-7"><h2 className="font-[family-name:var(--font-cormorant)] text-3xl">{person.name}</h2><p className="mt-1 text-sm text-[#f0d788]">{person.role}</p><p className="mt-4 leading-7 text-[var(--muted)]">{person.biography}</p></div></article>)}</div> : null}{publishedTestimonials.length ? <div className="mt-10 grid gap-6 md:grid-cols-2">{publishedTestimonials.map(item => <blockquote key={item.id} className="surface p-7"><p className="font-[family-name:var(--font-cormorant)] text-2xl leading-tight">“{item.quote}”</p><footer className="mt-5 text-sm text-[var(--muted)]">{item.name}{item.role ? ` · ${item.role}` : ""}{item.company ? `, ${item.company}` : ""}</footer></blockquote>)}</div> : null}</section> }
