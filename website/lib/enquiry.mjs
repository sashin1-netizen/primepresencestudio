const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export function validateEnquiry(input) { const errors = {}; if (!input.name?.trim()) errors.name = "Enter your name."; if (!emailPattern.test(input.email?.trim() || "")) errors.email = "Enter a valid email address."; if ((input.details?.trim() || "").length < 20) errors.details = "Tell us a little more—at least 20 characters."; if (!input.consent) errors.consent = "Confirm that we may respond to this enquiry."; return errors; }
export function buildEnquiryMailto(destination, input) { if (!emailPattern.test(destination)) throw new Error("A valid owner-approved destination email is required."); const subject=encodeURIComponent(`Project enquiry from ${input.name.trim()}`); const body=encodeURIComponent(`Name: ${input.name.trim()}\nBusiness: ${input.business || ""}\nEmail: ${input.email.trim()}\nTelephone: ${input.phone || ""}\nService: ${input.service || ""}\nBudget: ${input.budget || ""}\nTimeline: ${input.timeline || ""}\n\nProject:\n${input.details.trim()}`); return `mailto:${destination}?subject=${subject}&body=${body}`; }
