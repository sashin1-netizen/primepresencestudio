import fs from "node:fs";import {spawnSync} from "node:child_process";const npm=process.platform==="win32"?"npm.cmd":"npm";const finder=process.platform==="win32"?"where.exe":"which";const local=fs.existsSync("node_modules/lighthouse/lighthouse-cli/index.js")||fs.existsSync("node_modules/.bin/lighthouse.cmd")||fs.existsSync("node_modules/.bin/lighthouse");const global=spawnSync(finder,["lighthouse"],{encoding:"utf8"});const candidates=process.platform==="win32"?[`${process.env.PROGRAMFILES||""}\\Google\\Chrome\\Application\\chrome.exe`,`${process.env["PROGRAMFILES(X86)"]||""}\\Google\\Chrome\\Application\\chrome.exe`,`${process.env.LOCALAPPDATA||""}\\Google\\Chrome\\Application\\chrome.exe`]:["/usr/bin/google-chrome","/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"];const chrome=candidates.find(x=>x&&fs.existsSync(x));const registry=spawnSync(npm,["ping","--fetch-timeout=4000","--fetch-retries=0"],{encoding:"utf8",timeout:6000});console.log(`Local Lighthouse: ${local?"available":"unavailable"}`);console.log(`Global Lighthouse: ${global.status===0?global.stdout.trim():"unavailable"}`);console.log(`Chrome: ${chrome||"not detected"}`);console.log(`Registry: ${registry.status===0?"reachable":"unavailable or unreliable"}`);if(local||global.status===0){console.log("Automated: start production server, then lighthouse http://127.0.0.1:3000 --form-factor=mobile --output html --output json --output-path docs/artifacts/lighthouse/home-mobile");console.log("Repeat with --preset=desktop and routes /services, /work, /contact. Record route/profile/date in docs/artifacts/lighthouse-evidence.json.");process.exit(0)}console.log("Manual: Chrome Incognito → DevTools → Lighthouse → Navigation → Mobile → Performance, Accessibility, Best Practices, SEO → Analyze page load; repeat Desktop for /, /services, /work, /contact. Save fresh HTML and Chrome version/date.");console.log("Historical root lighthouse-report*.json files are excluded. No package file was modified.");process.exitCode=1;
