import fs from "node:fs";import path from "node:path";const file=path.resolve("docs/artifacts/device-test-record.json");const profiles=["Android Chrome","iPhone Safari","Windows Chrome","Windows Edge","Tablet portrait","Tablet landscape","Reduced motion","Slow connection","Keyboard-only","200% zoom"];
if(!fs.existsSync(file)){const record={generatedAt:new Date().toISOString(),tests:profiles.map(profile=>({profile,device:"",os:"",browser:"",browserVersion:"",viewport:"",tester:"",date:null,routesTested:[],result:"not-run",issues:[],screenshotPath:"",approvalStatus:"pending"}))};fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,`${JSON.stringify(record,null,2)}\n`);console.log(`Created ${file}. Every test is not-run; nothing was marked passed.`)}else{const record=JSON.parse(fs.readFileSync(file,"utf8"));for(const test of record.tests)console.log(`${test.profile}: ${test.result} / ${test.approvalStatus}`);const complete=record.tests.every(test=>test.result==="passed"&&test.approvalStatus==="approved"&&test.tester&&test.date);console.log(`Device sign-off: ${complete?"READY":"BLOCKED"}`);if(!complete)process.exitCode=1}
