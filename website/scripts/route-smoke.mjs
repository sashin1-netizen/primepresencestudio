const base=process.argv[2]||"http://127.0.0.1:3000"; const checks=[["/",200],["/services",200],["/work",200],["/about",200],["/process",200],["/contact",200],["/insights",200],["/review",404],["/privacy",404],["/terms",404],["/work/unpublished-project-check",404],["/unknown-route-check",404]]; let failed=0; for(const [route,expected] of checks){const response=await fetch(`${base}${route}`,{redirect:"manual"}); const csp=response.headers.get("content-security-policy")||""; const okay=response.status===expected; console.log(`${okay?"PASS":"FAIL"} ${route} ${response.status} expected ${expected}`); if(!okay)failed++; if(route==="/"&&(!csp||csp.includes("unsafe-eval"))){console.error("FAIL CSP missing or contains unsafe-eval");failed++;}} if(failed)process.exitCode=1;
