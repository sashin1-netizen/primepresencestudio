import test from "node:test"; import assert from "node:assert/strict"; import { isValidEmail,isValidTelephone,isValidWhatsApp,publicContact } from "../lib/contact-config.mjs";
const business=overrides=>({email:null,emailConfirmed:false,telephone:null,telephoneConfirmed:false,whatsapp:null,whatsappConfirmed:false,socialLinks:[],...overrides});
test("email requires one at-symbol and confirmation",()=>{assert.equal(isValidEmail("owner@example.test"),true);assert.equal(isValidEmail("owner@example@test.invalid"),false);assert.equal(isValidEmail(null),false);assert.equal(publicContact(business({email:"owner@example.test",emailConfirmed:false})).email,null);assert.equal(publicContact(business({email:"owner@example.test",emailConfirmed:true})).email,"owner@example.test")});
test("telephone requires confirmed international tel format",()=>{assert.equal(isValidTelephone("+27 71 234 5678"),true);assert.equal(isValidTelephone("0712345678"),false);assert.equal(isValidTelephone(null),false);assert.equal(publicContact(business({telephone:"+27 71 234 5678",telephoneConfirmed:false})).telephoneHref,null);assert.equal(publicContact(business({telephone:"+27 71 234 5678",telephoneConfirmed:true})).telephoneHref,"tel:+27712345678")});
test("WhatsApp requires confirmed international digits",()=>{assert.equal(isValidWhatsApp("27712345678"),true);assert.equal(isValidWhatsApp("+27 71 234 5678"),false);assert.equal(isValidWhatsApp(null),false);assert.equal(publicContact(business({whatsapp:"27712345678",whatsappConfirmed:false})).whatsapp,null);assert.equal(publicContact(business({whatsapp:"27712345678",whatsappConfirmed:true})).whatsapp,"27712345678")});
test("unconfirmed and invalid social links are excluded",()=>{const result=publicContact(business({socialLinks:[{label:"Draft",url:"https://example.test",confirmed:false},{label:"Bad",url:"javascript:alert(1)",confirmed:true},{label:"Good",url:"https://example.test",confirmed:true}]}));assert.deepEqual(result.socialLinks.map(item=>item.label),["Good"])});
